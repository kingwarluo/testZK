<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="SicGiveupReasonMapper">

    <!--          千万不要尝试修改该文件，该文件为模板基础，若表变更，重新执行Generator即可             -->
    <sql id="allFieldMap">
            id AS id,
            giveup_reason AS giveupReason,
            status AS status,
            create_emp AS createEmp,
            create_time AS createTime,
            modify_emp AS modifyEmp,
            modify_time AS modifyTime
    </sql>

    <sql id="AllFields">
            id,
            giveup_reason,
            status,
            create_emp,
            create_time,
            modify_emp,
            modify_time
    </sql>

    <select id="getByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicGiveupReasonExample" resultClass="com.mmc.sic.service.modules.domain.SicGiveupReason">
        SELECT
        <dynamic>
            <isNotNull property="distinct">
                DISTINCT
            </isNotNull>
            <isNotNull property="fields">
                <![CDATA[ $fields$ ]]>
            </isNotNull>
            <isNull property="fields">
                <include refid="allFieldMap" />
            </isNull>
            FROM t_sic_giveup_reason WHERE 1=1
            <isNotNull property="conditionSql">
                <![CDATA[ $conditionSql$ ]]>
            </isNotNull>
            <isNotNull  property="orderByClause">
                ORDER BY <![CDATA[ $orderByClause$ ]]>
            </isNotNull>
            <isNotNull  property="limit">
                <isNotNull  property="offset">
                    LIMIT #offset#, #limit#
                </isNotNull>
                <isNull property="offset">
                    LIMIT #limit#
                </isNull>
            </isNotNull>

        </dynamic>
    </select>

    <select id="getById" parameterClass="java.lang.Long" resultClass="com.mmc.sic.service.modules.domain.SicGiveupReason">
        SELECT
        <dynamic>
            <include refid="allFieldMap" />
            FROM t_sic_giveup_reason WHERE id = #id#
        </dynamic>
    </select>

    <insert id="insert" parameterClass="com.mmc.sic.service.modules.domain.SicGiveupReason">
        insert into t_sic_giveup_reason (<include refid="AllFields"/>)
        values (
        #id#,
        #giveupReason#,
        #status#,
        #createEmp#,
        #createTime#,
        #modifyEmp#,
        #modifyTime#
        )
        <selectKey resultClass="long" keyProperty="id">
            select LASt_INSERT_ID() AS id
        </selectKey>
    </insert>

    <insert id="insertBatch" parameterClass="java.util.Map">
        INSERT INTO t_sic_giveup_reason (<include refid="AllFields"/>)
        VALUES
        <iterate property="list" conjunction=",">
            (
                #list[].id#,
                #list[].giveupReason#,
                #list[].status#,
                #list[].createEmp#,
                #list[].createTime#,
                #list[].modifyEmp#,
                #list[].modifyTime#
            )
        </iterate>
    </insert>

    <select id="countByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicGiveupReasonExample" resultClass="java.lang.Long">
        SELECT
        <dynamic>
            COUNT(1) FROM t_sic_giveup_reason
            FROM t_sic_giveup_reason WHERE 1=1
            <isNotNull property="conditionSql">
                <![CDATA[ $conditionSql$ ]]>
            </isNotNull>
        </dynamic>
    </select>

    <update id="updateByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicGiveupReasonExample">
        UPDATE t_sic_giveup_reason SET
        <isNotNull property="updatedCondition">
            <![CDATA[ $updatedCondition$ ]]>
        </isNotNull>
        WHERE 1=1
        <isNotNull property="conditionSql">
            <![CDATA[ $conditionSql$ ]]>
        </isNotNull>
    </update>

    <delete id="deleteById" parameterClass="java.lang.Long">
        DELETE FROM t_sic_giveup_reason WHERE id = #id#;
    </delete>

</sqlMap>