<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="SicExpireMapper">

    <!--          千万不要尝试修改该文件，该文件为模板基础，若表变更，重新执行Generator即可             -->
    <sql id="allFieldMap">
            id AS id,
            configuration_item AS configurationItem,
            expire_hour AS expireHour,
            create_emp AS createEmp,
            create_time AS createTime,
            modify_emp AS modifyEmp,
            modify_time AS modifyTime
    </sql>

    <sql id="AllFields">
            id,
            configuration_item,
            expire_hour,
            create_emp,
            create_time,
            modify_emp,
            modify_time
    </sql>

    <select id="getByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicExpireExample" resultClass="com.mmc.sic.service.modules.domain.SicExpire">
        SELECT
        <dynamic>
            <isNotNull property="distinct">
                DISTINCT
            </isNotNull>
            <isNotNull property="fields">
                <![CDATA[ $fields$ ]]>
            </isNotNull>
            <isNull property="fields">
                <include refid="allFieldMap" />
            </isNull>
            FROM t_sic_expire WHERE 1=1
            <isNotNull property="conditionSql">
                <![CDATA[ $conditionSql$ ]]>
            </isNotNull>
            <isNotNull  property="orderByClause">
                ORDER BY <![CDATA[ $orderByClause$ ]]>
            </isNotNull>
            <isNotNull  property="limit">
                <isNotNull  property="offset">
                    LIMIT #offset#, #limit#
                </isNotNull>
                <isNull property="offset">
                    LIMIT #limit#
                </isNull>
            </isNotNull>

        </dynamic>
    </select>

    <select id="getById" parameterClass="java.lang.Long" resultClass="com.mmc.sic.service.modules.domain.SicExpire">
        SELECT
        <dynamic>
            <include refid="allFieldMap" />
            FROM t_sic_expire WHERE id = #id#
        </dynamic>
    </select>

    <insert id="insert" parameterClass="com.mmc.sic.service.modules.domain.SicExpire">
        insert into t_sic_expire (<include refid="AllFields"/>)
        values (
        #id#,
        #configurationItem#,
        #expireHour#,
        #createEmp#,
        #createTime#,
        #modifyEmp#,
        #modifyTime#
        )
        <selectKey resultClass="long" keyProperty="id">
            select LASt_INSERT_ID() AS id
        </selectKey>
    </insert>

    <insert id="insertBatch" parameterClass="java.util.Map">
        INSERT INTO t_sic_expire (<include refid="AllFields"/>)
        VALUES
        <iterate property="list" conjunction=",">
            (
                #list[].id#,
                #list[].configurationItem#,
                #list[].expireHour#,
                #list[].createEmp#,
                #list[].createTime#,
                #list[].modifyEmp#,
                #list[].modifyTime#
            )
        </iterate>
    </insert>

    <select id="countByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicExpireExample" resultClass="java.lang.Long">
        SELECT
        <dynamic>
            COUNT(1) FROM t_sic_expire
            FROM t_sic_expire WHERE 1=1
            <isNotNull property="conditionSql">
                <![CDATA[ $conditionSql$ ]]>
            </isNotNull>
        </dynamic>
    </select>

    <update id="updateByExample" parameterClass="com.mmc.sic.service.modules.repository.mapper.example.SicExpireExample">
        UPDATE t_sic_expire SET
        <isNotNull property="updatedCondition">
            <![CDATA[ $updatedCondition$ ]]>
        </isNotNull>
        WHERE 1=1
        <isNotNull property="conditionSql">
            <![CDATA[ $conditionSql$ ]]>
        </isNotNull>
    </update>

    <delete id="deleteById" parameterClass="java.lang.Long">
        DELETE FROM t_sic_expire WHERE id = #id#;
    </delete>

</sqlMap>